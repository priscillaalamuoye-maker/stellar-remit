import Dashboard from "@/components/Dashboard";

export const metadata = {
  title: "Dashboard | StellarRemit",
  description: "Batch cross-border payroll and remittance payouts on Stellar.",
};

export default function DashboardPage() {
  return <Dashboard />;
}
